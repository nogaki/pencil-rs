# CPU parity: multi-input, wisdom/threads, named collections, filters, catalogs

User approved all five categories. Baseline: main `ec9265d5cbbbcd2ba90fbd3b8a77e70ab0f112e4`, tree `e5a9d46496323cc649579527f0d1f338943669e2`. GPU/PR29 remains deferred and is not modified.

## Non-negotiable boundaries

Preserve old signatures, closed enums, file formats, defaults, numerical conventions, source/preflight atomicity and post-start ownership/poison behavior. All new collectives begin with the existing pair of five-u64 MIN/MAX reductions, before entry-specific collectives, invalid-input branching, allocation-dependent communication, or callbacks. No separate per-input preparation collectives.

Array remains FFT/I/O-independent, default FFT remains MPI-/FFTW-runtime-free, FFT remains unsafe-free; native unsafe is isolated. Exact Rust 1.85.0 is checked independently of normal stable. Each source worktree and MSRV run has its own target directory. Astra implements, Sol independently reviews, parent integrates/tests/publishes. No system installs, global configuration changes, secret inspection/staging/removal, or unrelated refactor. Existing September CI-success waiver does not mean CI passed.

## Deliverables

- [x] Multi-input computation: heterogeneous typed three-input pointwise/custom reduction plus arbitrary-count homogeneous view-slice APIs. Output-specified and in-place/view pointwise forms, no Clone/full input copies or per-point allocation. Same spatial layout, equal extra rank, singleton-extra expansion including 1 to 0; physical memory iteration. Empty many-input collections are rejected. New errors when needed rather than extending `PointwiseError`. Reductions have explicit communicator for empty-safe entry, composite descriptors for all inputs/output/neutral, preallocate all mapping/reference/partial storage before callbacks. Keep deterministic neutral-compatible associative reducer requirements, non-panicking/MPI-free callback contract and checked/IEEE/scaled standard many reductions. Stateful observation is distinct from a result depending on reducer invocation count/order. Any chosen panic policy must not introduce mismatched recovery collectives.
- [ ] FFTW wisdom and per-plan CPU threads: real import/export/forget APIs per sealed precision, strings plus standard file I/O examples. Pin successful base-library loads for process lifetime so imported wisdom persists after temporary handles/plans are dropped. Pin thread libraries too because initialization registers callbacks into base FFTW. Serialize initialization, wisdom mutations, planning and destruction under existing per-precision locks; no global cleanup routines. Preserve old `PlanOptions::new`, add validated requested thread count default one. Installed 3.3.8 thread libraries have init/set symbols but no planner-thread getter; restore known one via RAII, do not invent observed thread counts or foreign-state restoration. No speedup guarantee and no silent single-thread fallback. Exact thread count propagates into every local/distributed/core/collection descriptor before native factories, and survives direction/backend rebuild order. Old `FftwError`/planning enums stay closed; new wisdom error wrapper if required. Export pointers use matching FFTW deallocator and checked copying; all buffers initialized. Prove cache reuse after dropping all plans, not only while a library happens to remain loaded.
- [ ] Named collection I/O and options: standard view slices plus explicit communicator; one combined `[component, extra..., spatial...]` native dataset. Add named create/read/append and option-bearing collection paths without changing old defaults/signatures. Exact names/options/count/member checks and all required allocations agree before writes/duplicate lookup; reads stage all members through cleanup agreement. Empty collections reject collectively, no fake repeated writes or new collection owner framework. External writer serialization remains a caller obligation.
- [ ] HDF5 filters: extend private write options with shuffle and optional Deflate level 0..9 (zero still requests the filter). Require explicit valid chunks; verify each requested filter's availability and encode/decode capability collectively before native file open/create or mutation. Reject independent filtered writes explicitly. Apply actual chunk/shuffle/deflate DCPL in that order. Empty local selections still participate in collective writes when global extent is nonzero; globally empty datasets remain valid. No silent fallback, plugin registry, new compression dependency or resize API.
- [ ] Dataset catalog/metadata: collective read-only inspection with explicit Cartesian communicator and no array allocation prerequisite. Support existing single-dataset v1 and named layouts; return name/type/spatial shape/extras/writer provenance from bounded metadata only. New `DatasetInfo` private fields/getters and non-exhaustive unambiguous `ScalarType` (`ComplexF32`/`ComplexF64`). Strict snapshot semantics: malformed/incomplete named tail or HDF5 dataset makes the inspection fail, discarding staged results; no misleading complete partial catalog. Existing named reads can still recover known committed MPI-prefix records. No raw type inference, Julia-wire autodetection, or inferred `collection_count` (old formats store only extra dimensions). Enumerate HDF5 links by indexed queries rather than Rust allocation/unwind through C callbacks. Close all handles before publishing metadata or errors.

## Native and license context

Open MPI 4.1.7, parallel HDF5 1.10.7, native FFTW f32/f64 and pthread libraries are available. Loaded FFTW is 3.3.8, development headers/pkg-config 3.3.10, independent Julia reference FFTW 3.3.11; preserve that distinction. GPL/commercial FFTW notices remain and dynamic loading is not a license exemption. No GPU setup/validation is part of this batch.

## Verification

New checks must fail ignored-argument/no-op implementations. Cover multi-input arity/types/broadcast zeros, all-input validation and callbacks, neutral/type/count/layout mismatch and collective retry, IEEE/overflow/extremes; wisdom actual native round trip and precision isolation after all plans drop, invalid/NUL input, thread setter/reset on success/error/unwind and missing thread runtime, requested-count descriptor mismatches and real multi-thread numerical runs; named collection option/member/count/name mismatches with no partial destination or file mutation; actual HDF5 filters and invalid/unsupported options before creation, zero/uneven/complex datasets; strict catalog metadata, unknown formats, malformed/incomplete records, no payload reads and cleanup failures.

Run default/all-feature checks, fmt, Clippy, unit/rustdoc, exact 1.85.0, MPI 1/4/6, native FFTW and existing 87-fixture/312-configuration reference/corruption matrices. Parent verifies the combined tree. No general MPI/process-loss recovery, unmeasured speedup, or hardware/library capability is inferred from compilation.

## Array worktree implementation and verification

Implemented exports: `MultiInputError`; `pointwise3`, `pointwise3_views`, `pointwise3_in_place`, `pointwise3_in_place_views`; `pointwise_many`, `pointwise_many_views`, `pointwise_many_in_place`, `pointwise_many_in_place_views`; `map_reduce3`, `map_reduce_many`, `sum_many_by`, `norm_many_by`, `min_many_by`, `max_many_by`. View/owned-array methods also expose `map_reduce3`. Collective free functions take an explicit Cartesian communicator; many collective and pointwise-view inputs are view slices. Existing closed errors, signatures, and shared collective descriptor protocol remain unchanged.

New operation IDs: triple custom `0x4d523301`, many custom `0x4d524d01`, fixed many `0x41520010..=0x41520013` (sum/norm/min/max). New entries start with the common two five-u64 MIN/MAX reductions. Subsequent composite descriptor length/content and preparation agreements precede callbacks/payload. Custom reducers use local folds then rank-ordered AllGather/fold; standard reductions use existing checked/IEEE/scaled helpers. Callback panics are a documented contract violation with no recovery guarantee; no panic-catching recovery Allreduce or new abort wrapper was added.

Worktree-private evidence: `target/cpu4-logs/`, stable `target/cpu4-stable`, exact Rust 1.85.0 `target/cpu4-msrv-yKqvOk` (fresh unique directory at initial build). Commands: `cargo fmt --all -- --check`; `cargo clippy -p pencil-array --all-features --all-targets -- -D warnings`; separate `cargo test -p pencil-array --lib` and `--doc`; `cargo test -p pencil-array --all-features --tests --no-run`; exact `cargo +1.85.0 test -p pencil-array --tests --no-run`, `--lib`, and `--doc`. New `multi_input` and existing `collectives`/`array_ops` binaries run under `timeout 90 mpirun --oversubscribe -n {1,4,6}` on both toolchains, with executed markers. Stable alltoallv/array_access/timing_overlap also ran 1/4/6; array/local_transpose/many/pencil/topology ran supported 1/4 configurations. An extra attempted `array` run at six ranks failed its existing explicit 1/4-only guard; it is not counted as passing six-rank coverage. An initial combined `--lib --doc` command was rejected by Cargo; separate successful commands supersede it.

### Sol blocker follow-up

Restored `PointwiseError` to the exact original four-variant definition from `ec9265`; all four typed-three pointwise entry points now return `MultiInputError` with unboxed input attribution. An external exhaustive match guards the old closed enum. Removed the repeated `sum_many_by` panic-contract sentence without adding recovery collectives or changing opcodes/descriptor metadata.

The direct-view matrix covers all four typed-three/many output/in-place forms, nonempty `[1,3]`, `[2,1]`, `[1,1]` broadcasting to `[2,3]`, permuted spatial storage and callback order, rank-empty geometry, zero extras, empty-many rejection, heterogeneous non-Clone in-place data, and last-input validation without writes/callbacks. Typed-three reductions additionally cover nonempty broadcasting and rank-selected descriptor/type/neutral/layout/operation mismatch with successful retry and callback counters.

Follow-up evidence is `target/cpu4-logs/fix-*.log` (earlier unprefixed evidence predates this fix). Stable fmt, all-feature/all-target Clippy with `-D warnings`, all-feature test compilation, 37 unit tests and 12 rustdoc tests passed. Exact `cargo +1.85.0` all-feature test compilation, unit and rustdoc tests passed using the separate MSRV target above; version is logged in `fix-msrv-version.log`. `fix-mpi-summary.log` records 56 successful MPI launches on the final test sources: both toolchains run multi_input/collectives/array_ops/array_access/alltoallv_transpose/timing_overlap at 1/4/6, and array/local_transpose/many/pencil/topology at 1/4 only. Each launch used `timeout 90 mpirun --oversubscribe -n N BINARY --nocapture`; no legacy array six-rank run was made in this follow-up. Build environment used `CARGO_HOME=/tmp/pencil-rs-cargo-home`, build jobs 2, usual PATH/OpenMPI library path, and `RUSTUP_HOME=/tmp/pencil-rs-rustup` for exact MSRV. Initial helper compile/assertion failures were corrected before the final passing runs; no tests were skipped to obtain these results.

Limits: no allocator-failure injection or heap-profiler instrumentation; reusable fallibly preallocated reference scratch and no per-point allocation are verified structurally. No callback-panic recovery/process-loss guarantee, GPU work, FFT/I/O validation, or parent combined-tree verification is claimed.

## Work ownership

- Array worktree: multi-input APIs, private helpers, exports, tests, this plan.
- I/O worktree: named collections/options, filters, metadata/catalog inspection, FFI and tests.
- FFTW worktree: adapter wisdom/thread lifetime and consumer option/descriptor propagation, tests.
- Parent: README/NOTICE/CI integration, code review followups, full checks, controlled git publication.
